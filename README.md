# 密码管理器

<项目描述>

## 使用方法

先安装依赖：
windows双击 `安装依赖.bat`
linux或macos双击 `安装依赖.sh`

或者
```bash
pip3 install -r requirements.txt
```

启动应用：
windows双击 `启动.lnk` 失败则双击 `启动.bat`
linux或macos双击 `启动.sh`

调试时
windows双击 `启动_调试.bat`
linux或macos双击 `启动_调试.sh`
或者
```bash
python3 main.py
```
